<strong>
	{@maxLength key="rdfs:label" length="5"/}
{?get key="rdfs:label"}
	{@getValue key="rdfs:label"/}
{:else}
	{?id}
		{id}
	{:else}
		An entity
	{/id}
{/get}
</strong>
{?get key="rdfs:comment"}
	<p>{@maxLength key="rdfs:comment" length="5"/}</p>
{/get}